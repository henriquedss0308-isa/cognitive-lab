# Matriz de Reconciliação Crítica — Fable × GPT-5.6

Produção: `v1.2.0-pre-validation` (`05ef727b`) · Branch:
`review/fable-replication-critical-findings` · Evidência executável:
[`replication-oracle/`](../replication-oracle/) (48 checks).
Detalhamento em [REPLICACAO_CRITICA_FABLE.md](REPLICACAO_CRITICA_FABLE.md).

## Tabela consolidada

| Achado | Afirmação do GPT-5.6 | Reprodução feita | Resultado | Veredito | Sev. GPT | Sev. Fable | Confiança | Impacto histórico | Versionamento necessário | Ação segura recomendada |
|---|---|---|---|---|:--:|:--:|:--:|---|---|---|
| **AG-01** Corsi histórico | Scorers antigo/atual divergem sob a mesma `protocolVersion`; `scoringVersion` não separa baseline nem gráfico; resultado primário histórico comprometido | Blob legado real (`478a8fb^`, hash `8d8a030b`) **executado** contra o scorer atual em 6 cenários; série de 12 sessões em `computeBaselineStats`, `evaluatePrimaryZ` e `selectTrendSessions` | Divergência confirmada (ex.: acerto/erro/erro → legado 1, atual 2); atual coincide com o engine real; **nenhuma guarda** por `scoringVersion`; janela mistura as duas regras (mediana 3, MAD 0); gráfico exibe degrau 3→4 sem mudança de desempenho; `maxSpan` também diverge; pisos diferentes (1 vs 0) impedem correção aditiva | **CONFIRMADO COM RESSALVAS** — mecanismo provado; "usado normalmente" não demonstrado | P0 | **P1** (→ P0 se o inventário achar ≥1 sessão Corsi pré-`478a8fb`) | alta (mecanismo) / baixa (incidência real) | Restrito ao Corsi, fronteira de 18 dias (2026-07-02 a 07-20). Trials brutos intactos ⇒ **reprocessável** | **Não** bumpar `protocolVersion` (estímulo/engine inalterados). Usar o `scoringVersion` já existente como chave de partição | Inventariar sessões Corsi por `scoringVersion` **sem escrever**; exportar backup; reprocessar em cópia com trilha de auditoria; reconstruir baseline Corsi só depois; até lá, suprimir z e tendência do Corsi se a série for mista |
| **AG-03** Antecipações × SDT | Pré-onset em Go não vira hit nem miss, em No-Go vira FA; `H+M ≠ N_sinal`; sessão segue válida; d′ muda materialmente; atinge Go/No-Go, n-back e SART | Trials construídos por `buildTrialRecord` (fluxo real) e pontuados pelos `scoreSession` de produção; 8 configurações por teste; magnitudes de d′ calculadas sob três políticas | Assimetria **confirmada** (`H+M=111` vs `N_sinal=120`; `FA+CR` sempre fecha). Mas antecipação **pós-onset conta como hit** e não quebra nada — só pré-onset quebra. Sessão permanece `valid` a 11,25%; acima de 15% só vira `valid_with_warnings`. Δd′: 0,05 (2 pré-onset, realista) · 0,21 (9, fixture do GPT) · **0,30 no n-back** (3 pré-onset, ~27 alvos). No SART, pré-onset em No-Go **infla a primária** `commissionErrorRate` | **CONFIRMADO COM RESSALVAS** — mecanismo real; alcance mais estreito e pior caso diferente do alegado | P1 | **P1** | alta (mecanismo) / média (magnitude — janela física de ~33 ms, 2 rAF) | Afeta `dPrime` (gonogo), `dPrime2Back` (nback), `dPrime` + `commissionErrorRate` (sart) em sessões com resposta pré-onset. Não corrompe trials | Nova `scoringVersion` **se** a política mudar. Exclusão simétrica preserva **exatamente** o d′ atual no lado Go ⇒ ruptura histórica mínima | Declarar a política **antes** de codificar (miss/FA · exclusão simétrica · invalidação por limiar); medir a frequência real de pré-onset nos dados antes de estimar impacto; particionar/reconstruir só os baselines que a política escolhida alterar |
| **AC-12** Unidade, escala e arredondamento | Interface mostra proporção SART como `0.11 ms` e d′ arredondado como inteiro com `ms`; por estar no cartão principal, é P1 | `MetricCard` **montado** (`@testing-library/react`), texto lido do DOM, com as expressões de unidade de `Results.tsx:237/:298` como fixture; 20 casos incl. zero, null, negativo, extremo | Confirmado e **ampliado**: `d′ 2,4 → "2 ms"`, `d′2-back 1,87 → "2 ms"`, `commissionErrorRate 0,11 → "0.11 ms"` e — não visto por nenhuma revisão anterior — **`confirmedSpan 5 → "5 ms"`**, porque `'confirmedSpan'.includes('span')` é `false` (o guard de span é código morto). **4 dos 8 testes**. Perda de resolução: 2,4 e 2,49 renderizam igual; `criterion 0,42 → "0"`. Gráfico (`formatTrendValue`) está correto ⇒ cartão e gráfico **discordam na mesma sessão**. Valor persistido e z **intactos** | **CONFIRMADO E AMPLIADO** | P1 (3 testes) | **P1** (4 testes) | alta | **Nenhum** — só apresentação; dados persistidos corretos e recuperáveis | **Nenhum**. Não requer `scoringVersion`, `protocolVersion` nem reconstrução de baseline | Substituir as heurísticas por substring por uma tabela explícita de formato por métrica (unidade, casas, fator ×100). É a correção de **menor risco e maior retorno imediato** dos três |
| **AC-02** Fallback de métrica primária | P1; alcança Stroop e Task Switching, não os seis testes | 6 checks: sessões degeneradas mas legítimas geradas pelos `scoreSession` reais; z avaliado contra baseline sintético de custo | Fallback existe e cruza grandezas: Stroop com `stroopCostRT = null` exibe RT ≈ 600 ms e produz **\|z\| > 10** contra baseline de custo (mediana 90 ms), com sessão **não** `invalid`. Idem Task Switching. Go/No-Go, SART, n-back e Corsi **não** alcançam (Hautus mantém d′ finito; gerador SART sempre tem No-Go; `0 ?? x === 0`). simple_rt/choice_rt inócuos | **CONFIRMADO** — escopo do GPT-5.6 correto; corrige a auditoria anterior (Fable) | P1 | **P1** | alta | Nenhum sobre dados persistidos — o valor gravado da primária permanece `null` corretamente | Nenhum | Remover o `?? medianCorrectRT`; o caminho `value_missing` de `evaluatePrimaryZ` já produz a mensagem correta sozinho |

## Reconciliação de severidade

| Achado | Auditoria Fable v1 | Revisão GPT-5.6 | **Replicação Fable** | Motivo da posição final |
|---|:--:|:--:|:--:|---|
| AG-01 | não detectado | P0 | **P1 → P0 condicional** | Mecanismo provado por execução do blob real; incidência histórica indeterminada sem backup. Severidade não deve ser afirmada a partir de série sintética |
| AG-03 | não detectado | P1 | **P1** | Assimetria confirmada em 3 testes, todos com métrica primária atingida; magnitude do GPT vem de fixture pouco plausível, mas o n-back é pior do que ele estimou — os erros se compensam e P1 permanece proporcional |
| AC-12 | P2 | P1 | **P1** | Caminho inevitável em 4 de 8 testes na tela principal, sem exigir dado degenerado. Não é P0: nada é corrompido |
| AC-02 | P1 (escopo inflado) | P1 (2 testes) | **P1 (2 testes)** | Escopo do GPT-5.6 verificado e adotado |

## Ordem de correção sugerida (não implementada)

1. **AC-12** — sem migração, sem versionamento, corrige leitura falsa hoje.
2. **AC-02** — remoção de um `??`; a mensagem correta já existe no código.
3. **AG-01** — inventário primeiro; migração só com backup e trilha de auditoria.
4. **AG-03** — decisão normativa documentada antes de qualquer código.
