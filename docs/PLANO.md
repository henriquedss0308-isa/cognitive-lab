# Plano Executável — Correções P0/P1

Comandos de verificação padrão (rodar após CADA commit): `npm test` · `npx tsc -b` · `npm run lint` · `npm run build`.
Rollback padrão: `git revert <sha>` (commits pequenos e independentes; exceções anotadas).
Branch de trabalho: `audit/p0-p1`.

| # | Commit | Objetivo | Arquivos | Schema/Migração | Testes novos | Critério de aceite | Rollback |
|---|---|---|---|---|---|---|---|
| C0 | `chore: snapshot da edição de condições (WIP pré-auditoria)` | Preservar em histórico o trabalho local não commitado encontrado no working tree | TestConditionsForm, AppContext, Results, repository, SITUACAO_PROJETO.txt | — | — (já coberto) | diff vazio vs working tree original | revert |
| C1 | `docs: auditoria + especificação + plano` | Entregas 1–3 (documentos) | docs/*.md | — | — | docs presentes | revert |
| C2 | `fix(P0-2/P0-3): direção explícita de métricas e z nulo-seguro` | z com sinal correto (d′, span) e sem `?? 0` | tests/types.ts, 8 defs de teste, statistics/zscore.ts (novo), Results.tsx | — | metricDirections.test (cobertura+semântica), zscore.test | z>0 = melhor em TODOS os testes; métrica nula ⇒ z null | revert |
| C3 | `fix(P0-1): fase do baseline sem off-by-one + migração v3 de rótulos` | Rótulo = fase real (fam 1–3, building 4–11, monitoring 12+); fonte única de elegibilidade | TestFlow.tsx, statistics/baseline.ts (recomputeBaselinePhase), storage/db.ts (v3) | DB v3: reescreve `result.baselinePhase` (determinístico, reversível por recomputo) | baseline.phase.test (fronteiras 0,2,3,10,11,12; filtro único) | 3ª sessão=familiarization; 11ª=building; 12ª=monitoring; rótulos antigos migrados | revert do código; rótulos re-migráveis (função pura versionada) |
| C4 | `fix(P0-4): resume preserva check-in, bateria e dispositivo original` | Conclusão faz merge com registro existente | TestFlow.tsx, storage/sessionCompletion.ts (mergeCompletionRecord) | — | mergeCompletionRecord.test | resume mantém checkIn/battery/startedAt/deviceInfo; divergência de dispositivo ⇒ flag+aviso | revert |
| C5 | `fix(P0-5): import validado, idempotente e não destrutivo` | Validação estrita por sessão; skip-existing; relatório; settings preservados; JSON robusto | storage/export.ts, storage/repository.ts, pages/Settings.tsx | — (formato de RELATÓRIO novo, backup inalterado) | importBackup.validation.test (malformado, enum inválido, duplicado idempotente, relatório) | reimport não altera dados locais; malformado rejeitado por item com motivo; JSON inválido não explode | revert (import antigo volta) |
| C6 | `fix(P1-11): transições terminais protegidas + ESC inerte pós-done` | `completed/abandoned` imutáveis via updateSessionStatus; ESC ignorado após done | storage/repository.ts, components/test/TestRunner.tsx | — | statusTransitions.test | updateSessionStatus em completed = no-op; ESC pós-done não gera abandoned | revert |
| C7 | `fix(P1-7/P1-8): zumbis viram interrupted; descartar arquiva em vez de deletar` | Marcação na inicialização; delete só explícito | storage/repository.ts (markStaleInProgress), context/AppContext.tsx, pages/Settings.tsx | — | staleSessions.test | reload deixa de gerar in_progress eterno; "Descartar" preserva dados; "Excluir permanentemente" separado c/ confirm | revert |
| C8 | `fix(P1-6): falha de persistência interrompe com mensagem` | Erro de IndexedDB mid-sessão ⇒ estado de erro visível + interrupted | components/test/TestRunner.tsx, pages/TestFlow.tsx | — | (unit do handler puro) | rejeição de append não congela silenciosamente | revert |
| C9 | `feat(P1-2/P1-14): transparência do baseline (composição, n mínimo, MAD=0)` | Exibir "N (K com avisos)"; z exige n≥6; MAD=0 explicado + delta bruto | statistics/baseline.ts (contagem warnings), statistics/zscore.ts, Results.tsx | — | baselineComposition.test, zscore n/MAD tests | monitoring com MAD=0 mostra explicação, nunca z; n<6 suprime z com motivo | revert |
| C10 | `fix(P1-5/P1-12): gráficos e utilitários sem contaminação` | Tendências excluem invalid; SpeedAccuracy exclui demo; getLatestConditions !demo; z não exibido para sessão demo | components/charts/SessionCharts.tsx, storage/repository.ts, pages/Results.tsx | — | chartFilters.test (seletor extraído) | sessão invalid fora da tendência; demo fora de scatter/condições/z | revert |
| C11 | `fix(P1-3): scoring Corsi por replay do engine` | confirmedSpan/término derivados de applyCorsiResult; instruções alinhadas | tests/corsi/index.ts, tests/corsi/adaptive.ts (replay), instruções | scoringVersion → `sdt-hautus-1;corsi-replay-1` só em sessões novas | corsiReplay.test (engine≡scoring em cenários divergentes) | para o MESMO conjunto de trials, scoring == estado final do engine | revert (sessões antigas intactas — resultados congelados) |
| C12 | `feat(P1-10): sinalização de dispositivo divergente` | Comparar deviceType/inputMethod com moda do baseline na conclusão | pages/TestFlow.tsx (helper puro deviceComparison.ts) | — | deviceComparison.test | sessão em dispositivo divergente ⇒ warning visível, quality=valid_with_warnings | revert |
| C13 | `feat(P1-9): antecipações em fixação/ISI contabilizadas` | earlyPressCount por trial (metadata) + agregado por sessão; sem criar trial | components/test/TestRunner.tsx, scoring/common.ts (agregado) | metadata aditivo (retrocompatível) | earlyPress.test (agregador puro) | pressionar em ISI aparece como `isiEarlyPresses` na sessão nova; sessões antigas inalteradas | revert |

Notas de risco de regressão:
- C3 é o único com migração; ela reescreve apenas `result.baselinePhase` (rótulo derivável) — perda impossível, reexecutável.
- C5 muda contrato do import (antes: sobrescrever; depois: skip) — comportamento antigo era o bug (P0-5); documentado no README.
- C11 altera scoring de sessões NOVAS do Corsi; antigas mantêm resultado congelado (invariante I6). Fronteira anotável via scoringVersion.
- C13 não muda anticipationRate (comparabilidade preservada, conforme spec §14).
